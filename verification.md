# 验证清单

> 对照 test.md，逐项验证。通过打 ✔，失败打 ✘，附简要说明。

## 已知问题（待后续修复）

- [ ] **光标对齐**：textarea 定位与实际字符位置有偏差，光标显示在字符上方/附近而非精确位置
- [ ] **光标大小**：textarea 尺寸 2px×1em，光标显示为细线而非正常输入光标高度
- [ ] 以上两项需后续精确调整 `pointFromCaret` 坐标计算和 textarea 尺寸

---

## 1. 文本样式

- [ ] 点击普通段落，光标定位正确
- [ ] 输入文字，实时回写，source 同步
- [ ] **粗体** 渲染为粗体样式
- [ ] 点击粗体区域，编辑后 `**` 标记不丢失
- [ ] *斜体* 渲染为斜体样式
- [ ] ***粗斜体*** 渲染为粗斜体样式
- [ ] ~~删除线~~ 渲染为删除线 + 浅色
- [ ] `行内代码` 渲染为等宽灰底样式
- [ ] 删除字符，回写正确

---

## 2. 标题

- [ ] h1 渲染为 `text-2xl font-bold`
- [ ] h2 渲染为 `text-xl font-semibold`
- [ ] h3 渲染为 `text-lg font-medium`
- [ ] h4-h6 渲染为对应字号
- [ ] 标题不显示 `#` 前缀
- [ ] 点击标题，光标定位到精确字符位置
- [ ] 编辑标题文字，回写后 `#` 标记不变
- [ ] 标题末尾 Enter，下方出现正文大小空行
- [ ] 标题中间 Enter，后半段变为正文段落
- [ ] 标题 Enter 后新行可编辑

---

## 3. 链接和图片

- [ ] `[GitHub](url)` 渲染为蓝色可点击链接，`[]()` 不显示
- [ ] 自动链接 `https://...` 显示为纯文本（暂无自动转换）
- [ ] `![Markdown](url)` 图片正常加载显示

---

## 4. 引用块

- [ ] 单层 `>` 渲染左边框 + 浅色文本
- [ ] `>` 前缀不显示
- [ ] 嵌套 `> >` 显示双层缩进
- [ ] 点击引用块，光标定位正确
- [ ] 编辑后回写保持 `> ` 前缀
- [ ] 引用块内 Enter，新行自动续 `> `
- [ ] 引用块 Backspace 删除字符正常
- [ ] 引用块 Delete 合并正常

---

## 5. 列表

- [ ] 无序 `-` 渲染为 ul + 圆点
- [ ] 有序 `1.` 渲染为 ol + 数字
- [ ] 嵌套列表显示子级缩进
- [ ] 任务 `- [x]` 显示勾选 checkbox
- [ ] 任务 `- [ ]` 显示未勾 checkbox
- [ ] 点击列表项，光标定位正确
- [ ] 编辑回写保持 `-` / `1.` 前缀
- [ ] 列表 Enter，新行续前缀
- [ ] 列表 Tab 缩进 2 空格
- [ ] 列表 Shift+Tab 退层 2 空格

---

## 6. 表格

- [ ] 表头 + 数据行渲染为 table 网格
- [ ] 格内 `**粗体格**` 内联渲染正常
- [ ] 格内 `` `代码格` `` 等宽渲染正常
- [ ] 点击表格，光标能定位（displayText 丢列边界为已知限制）
- [ ] 编辑格内文字，回写保持 `|` 分隔符

---

## 7. 代码块

- [ ] JavaScript 围栏代码块暗底亮字
- [ ] Python 代码块暗底亮字
- [ ] 语言标签显示（`javascript` / `python`）
- [ ] 纯文本围栏代码块正常显示
- [ ] 点击代码块，光标定位
- [ ] 编辑回写保持 ``` 围栏
- [ ] 代码块 Enter 行内换行不拆分块

---

## 8. HTML

- [ ] `<details>` 展开/折叠渲染
- [ ] `<kbd>` 键盘按键样式渲染
- [ ] `<mark>` 高亮文本渲染

---

## 9. 水平线和转义

- [ ] `---` 渲染为 `<hr>` 水平线
- [ ] `\*不是斜体\*` 反斜杠转义，不渲染为斜体
- [ ] `&copy;` `&lt;` HTML 实体显示为对应字符

---

## 10. 混合场景

- [ ] 引用中包含 **粗体** `` `代码` `` [链接]()
- [ ] 列表中嵌套代码块（纯文本显示）
- [ ] 列表中嵌套引用
- [ ] 列表中嵌套任务列表

---

## 11. 键盘操作（全局）

- [ ] 点击任意块，textarea 定位正确，获得焦点
- [ ] 字符输入即时回写，WYSIWYG 与 source 同步
- [ ] Enter 新行正确，光标移到新行
- [ ] Backspace 删字符正常
- [ ] Backspace 空行行首：删除该行，光标移到上一行末尾
- [ ] Backspace 非空行行首：合并到上一行末尾
- [ ] Delete 删字符正常
- [ ] Delete 行末：合并下一行到当前行
- [ ] ← → 块内移动光标
- [ ] ↑ ↓ 跨块移动 + 保持列位置
- [ ] Ctrl+Z 撤销可用，重渲染后光标位置正确
- [ ] Ctrl+Y 重做可用
- [ ] 中文 IME 输入正常，composition 不丢字

---

## 12. 模式切换

- [ ] WYSIWYG → Source 模式，content 一致
- [ ] Source 编辑 → WYSIWYG，渲染更新
- [ ] WYSIWYG → Preview 模式，react-markdown 渲染正常

---

## 汇总

| 分组 | 通过 | 失败 | 备注 |
|------|------|------|------|
| 1. 文本样式 | / | / | |
| 2. 标题 | / | / | |
| 3. 链接图片 | / | / | |
| 4. 引用块 | / | / | |
| 5. 列表 | / | / | |
| 6. 表格 | / | / | |
| 7. 代码块 | / | / | |
| 8. HTML | / | / | |
| 9. 水平线转义 | / | / | |
| 10. 混合场景 | / | / | |
| 11. 键盘操作 | / | / | |
| 12. 模式切换 | / | / | |
